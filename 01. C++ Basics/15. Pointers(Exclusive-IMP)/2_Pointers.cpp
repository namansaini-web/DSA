#include<iostream>
using namespace std ; 
int main(){
    int a = 5 ;
    int* ptr = &a ;  // Storing addess of a in a pointer which also have its own address //
    cout<< &a <<endl ;
    cout<< ptr <<endl ;
    cout<< &ptr <<endl ;

    // int* --> datatype which stores the address of int variable.

    // Syntax: data_type* pointer_name //  
}